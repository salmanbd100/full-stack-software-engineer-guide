% book-header.tex — the PDF preamble, and nothing else.
%
% Improvement #79 turned this file from the whole print design into a shim. The design now
% lives in five files under scripts/tex/, loaded in dependency order, and the split is what
% makes #77's calibration a one-file edit:
%
%   tokens.tex      every tunable value — palette, type scale, geometry, leading, rules.
%                   **The only file calibration touches.**
%   typography.tex  the three vendored families and the body text settings
%   glyphs.tex      the emoji substitution table
%   structure.tex   page geometry, running heads, part and chapter openings
%   blocks.tex      callouts, tables, code, pull quotes  (filled at #81)
%
% Order matters: everything after tokens.tex reads the names it defines, and blocks.tex
% needs the fonts typography.tex loads.
%
% The paths are relative to the repository root, which is where build-book.sh runs
% tectonic from. Adding a `cd` to that script breaks every line below.

\input{scripts/tex/tokens.tex}
\input{scripts/tex/typography.tex}
\input{scripts/tex/glyphs.tex}
\input{scripts/tex/structure.tex}
\input{scripts/tex/blocks.tex}
