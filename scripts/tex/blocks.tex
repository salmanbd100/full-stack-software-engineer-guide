% blocks.tex — the black-and-white block library.
%
% Empty at improvement #79 by design: this file exists so that the five-file split is
% complete and `book-header.tex` has its final shape, and **#81 fills it**.
%
% What lands here: the three callout types distinguished by structure rather than by fill
% (a full box, top-and-bottom rules, and a solid left bar), the metadata pill, the table
% treatment, the code block, and the pull quote — built with `tcolorbox` and `tabularray`,
% driven by a `scripts/lua/callouts.lua` filter so no markdown changes.
%
% Nothing is declared here yet. A placeholder environment would be a thing to delete
% later, and #81 has the design in front of it.
